import useReducer  from "react"
import { reducer } from "./Actionstate"

export function ActionState(intialstate){
    const [state,dispatch] = useReducer(eeducer)
    const increment  =()=>{
        
    }
}